# Интеграция AI для голосовых звонков через Voximplant

## Что было сделано

Ваш backend теперь настроен для **разговоров с ИИ** через Voximplant + OpenAI! 🎉

### Реализовано:

1. **Voximplant Сценарий** для записи и обработки аудио
   - `voximplant_scenario_STREAMING.js` - работает с записью речи пользователя
   - `voximplant_scenario_AI_BRIDGE.js` - для продвинутой интеграции через Node.js bridge

2. **Python Backend Endpoints**:
   - `POST /voximplant/process_audio` - обрабатывает аудио от Voximplant через OpenAI
   - Автоматическая активация streaming mode для всех звонков

3. **OpenAI Integration**:
   - Whisper (STT) - транскрипция речи пользователя
   - GPT-4 - генерация ответов ИИ
   - TTS - синтез речи (в сценарии используется Voximplant TTS)

---

## Архитектура

### Текущая работающая схема (HTTP-based):

```
┌─────────────┐         ┌──────────────┐         ┌─────────────┐         ┌──────────┐
│   Абонент   │ ◄─────► │  Voximplant  │ ◄─────► │   Backend   │ ◄─────► │  OpenAI  │
│   (PSTN)    │  Audio  │   VoxEngine  │   HTTP  │   (Python)  │  API    │   APIs   │
└─────────────┘         └──────────────┘         └─────────────┘         └──────────┘
```

**Поток разговора:**

1. **Звонок соединен** → Voximplant говорит приветствие
2. **Пользователь говорит** → Voximplant записывает речь (до паузы 2 сек)
3. **Запись закончена** → Voximplant отправляет audio_url в Backend
4. **Backend скачивает аудио** → отправляет в OpenAI Whisper (STT)
5. **Whisper возвращает текст** → Backend отправляет в GPT-4
6. **GPT-4 генерирует ответ** → Backend возвращает текст в Voximplant
7. **Voximplant произносит ответ** → используя TTS
8. **Цикл повторяется** → до завершения разговора

---

## Как запустить

### 1. Обновите сценарий в Voximplant

```bash
# Загрузите новый сценарий в Voximplant
python update_to_streaming_scenario.py
```

Или вручную через Voximplant Console:
1. Откройте Voximplant Console
2. Перейдите в Applications → Scenarios
3. Создайте новый сценарий "OutboundCallStreaming"
4. Скопируйте содержимое `voximplant_scenario_STREAMING.js`
5. Обновите правило (Rule) чтобы использовать новый сценарий

### 2. Проверьте настройки Backend

Убедитесь что в `.env` есть:

```bash
# OpenAI
OPENAI_API_KEY=sk-...

# Voximplant
VOXIMPLANT_ACCOUNT_ID=10042950
VOXIMPLANT_API_KEY=ваш_api_key
VOXIMPLANT_APPLICATION_ID=47277483
VOXIMPLANT_RULE_ID=ваш_rule_id
VOXIMPLANT_CALLER_ID=+78652594087
BACKEND_URL=https://ваш-ngrok-url.ngrok-free.app

USE_VOXIMPLANT=true
```

### 3. Запустите Backend

```bash
# Активируйте виртуальное окружение (если есть)
# Запустите сервер
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 4. Сделайте тестовый звонок

```bash
curl -X POST http://localhost:8000/calls \
  -H "Content-Type: application/json" \
  -d '{"phone": "+79019433546"}'
```

---

## Логи и отладка

### Проверьте логи Voximplant:
1. Откройте Voximplant Console
2. History → Call Sessions
3. Найдите ваш звонок
4. Посмотрите Session Transcript

### Проверьте логи Backend:
```bash
tail -f logs/app.log
# Или просто смотрите консоль где запущен uvicorn
```

Ожидаемые логи:
```
[Voximplant Audio] Processing audio | call_id=xxx | url=https://... | duration=3.2s
[Voximplant Audio] Downloaded 51234 bytes
STT completed in 0.523s | text_length=45
[Voximplant Audio] User said: Здравствуйте, хочу узнать о ваших услугах
GPT completed in 1.234s | tokens=234 | response_length=89
[Voximplant Audio] AI response: Здравствуйте! С удовольствием расскажу...
```

---

## Альтернативная архитектура (Real-time WebSocket)

Для более продвинутой интеграции с **OpenAI Realtime API** (реальный streaming):

### Используйте сценарий `voximplant_scenario_AI_BRIDGE.js`

Этот сценарий требует:
1. **Node.js Bridge Server** (уже есть в `voximplant-bridge/`)
2. **Voximplant WebSDK** для подключения Bridge к VoxEngine
3. **WebSocket** соединение Backend ↔ Bridge ↔ VoxEngine

**Преимущества:**
- ⚡ Реальный real-time streaming
- 🎙️ Естественные диалоги без задержек
- 🔄 OpenAI Realtime API с VAD (Voice Activity Detection)

**Недостатки:**
- Сложнее в настройке
- Требует запуск Node.js сервера
- Voximplant WebSDK работает только в браузере (нужен Puppeteer)

---

## Следующие шаги

### Для улучшения текущей реализации:

1. **Добавить контекст разговора**
   - Сохранять историю диалога в `call_session.transcript`
   - Передавать весь контекст в GPT для более осмысленных ответов

2. **Улучшить промпты**
   - Отредактировать `SYSTEM_PROMPT` в `app/integrations/openai_client.py`
   - Добавить инструкции специфичные для вашего бизнеса

3. **Оптимизировать задержки**
   - Использовать streaming responses от GPT
   - Кешировать типовые ответы

### Для перехода на Real-time Streaming:

1. **Запустите Node.js Bridge**:
   ```bash
   cd voximplant-bridge
   npm install
   node index.js
   ```

2. **Обновите Voximplant сценарий** на `voximplant_scenario_AI_BRIDGE.js`

3. **Настройте WebSocket endpoints** в Backend

---

## Тестирование

### Проверьте что работает:

1. ✅ Звонок проходит
2. ✅ Приветствие проигрывается
3. ✅ Пользователь может говорить
4. ✅ Voximplant записывает речь
5. ✅ Backend получает audio_url
6. ✅ OpenAI транскрибирует речь
7. ✅ GPT генерирует ответ
8. ✅ Voximplant произносит ответ
9. ✅ Цикл повторяется

### Типичные проблемы:

**"Backend не получает audio_url"**
- Проверьте что `BACKEND_URL` правильный и доступен из интернета
- Используйте ngrok для локальной разработки

**"OpenAI API ошибка 401"**
- Проверьте `OPENAI_API_KEY`
- Убедитесь что у вас есть credits

**"Voximplant не может скачать audio"**
- Voximplant записи хранятся временно
- URL действителен ~24 часа

**"Долгие задержки между репликами"**
- Это нормально для HTTP-based подхода (2-5 секунд)
- Для реального real-time используйте WebSocket архитектуру

---

## Дополнительные ресурсы

- [Voximplant Documentation](https://voximplant.com/docs/)
- [OpenAI Whisper API](https://platform.openai.com/docs/guides/speech-to-text)
- [OpenAI GPT-4 API](https://platform.openai.com/docs/guides/chat)
- [OpenAI Realtime API](https://platform.openai.com/docs/guides/realtime)

---

## Поддержка

Если возникли вопросы:
1. Проверьте логи Backend и Voximplant
2. Убедитесь что все API keys правильные
3. Проверьте что ngrok tunnel активен
4. Посмотрите примеры запросов в `/docs` endpoint

Удачи! 🚀
